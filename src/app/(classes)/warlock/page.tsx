import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Warlock",
};

export default function Home() {
    return (
        <>
            <p>Warlock</p>
        </>
    );
}
